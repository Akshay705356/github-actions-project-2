name: initial-deploy-with password
on: workflow_dispatch
env:
    MONGODB_DB_NAME: "gh-actions-db"
jobs:
  test:
    env:
      MONGODB_PASSWORD: Shuru@2711
      MONGODB_USERNAME: akshay705356
      MONGODB_CLUSTER_ADDRESS: cluster0.sh3q3.mongodb.net
      PORT: 3000
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v4
      - name: Cache dependencies
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: npm-deps-${{ hashFiles('**/package-lock.json') }}
      - name: Install dependencies
        run: npm ci
      - name: Run server
        run: npm start & npx wait-on http://127.0.0.1:XYZ
      - name: Run tests
        run: npm test
      - name: Output information
        run: echo "..."
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Output information
        run: |        
          echo "..."
